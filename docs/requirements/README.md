# 要件定義

「何を作るか」「なぜ作るか」「どこまでを初期スコープとするか」を定義するドキュメントです。
実装方法(アーキテクチャ・データ構造など)は `../design/` に、技術選定の理由は `../adr/` に記載します。

## 命名規則

`NNN-対象名.md`（例: `001-mvp-requirements.md`）

要件が大きく変わった場合は既存ファイルを上書きせず、新しい版として追記・更新履歴を残してください。
