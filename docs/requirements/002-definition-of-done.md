# 002. 実装完了の定義(Definition of Done)

- 作成日: 2026-09-21
- 更新日: 2026-09-21
- 作成者: A Citizen for the Association
- ステータス: Approved(2026-09-21時点で全項目達成。[テスト結果レポート](../reports/2026-09-21-1-test-results.md)参照。CI(2.5)は実際のGitHub Actions上でグリーンになったことを確認済み)
- 関連: [要件定義 001](./001-mvp-requirements.md) / [コントラクト基本設計書](../design/001-contract-design.md) / [フロントエンド基本設計書](../design/002-frontend-design.md)

## 1. 目的

初期バージョン(MVP)の実装が「完了した」と判断するための客観的な基準を定義する。ここに定義した基準を満たすまでは完了として扱わない。

## 2. 完了条件

### 2.1 機能実装

- [x] [コントラクト基本設計書](../design/001-contract-design.md)に記載された`Membership.sol`・`GovernanceToken.sol`・`CommunityBoard.sol`の全関数が実装されている
- [x] [フロントエンド基本設計書](../design/002-frontend-design.md)に記載された全画面・全コンポーネントが実装されている

### 2.2 コントラクトのテスト

- [x] Foundryでテストコードを実装し、`forge test`が全件成功する(2026-09-21時点で62件全成功)
- [x] `forge coverage --no-match-coverage "script/"`のカバレッジが、`src/`配下のコントラクトについて **line / statement / branch / function すべて100%**(2026-09-21時点で達成)
  - `script/`(デプロイスクリプト)は対象外とする。ユニットテストではなく実際のデプロイ(dry-run含む)で動作確認するものであるため([コントラクト設計書 7章](../design/001-contract-design.md)参照)。
  - それ以外で100%達成が現実的に困難な箇所(例: Solidityコンパイラが自動生成する到達不能コード等)がある場合は、この文書に理由と該当箇所を明記した上で除外する。理由を示さない未達は認めない。

### 2.3 フロントエンドのテスト

「行カバレッジ100%」は採用しない(理由は3章)。代わりに以下の3つを完了基準とする。

- [x] **ユニット/コンポーネントテスト**: `lib/`配下の純粋ロジック・カスタムフックのライン/ブランチカバレッジ80%以上(Vitest + Testing Library)。2026-09-21時点で45件全成功、`lib/`+`hooks/`のline/statement/functionカバレッジ100%、branchカバレッジ91.07%(いずれも80%基準を達成)。
- [x] **E2Eゴールデンパス**: 4章に列挙する全シナリオが、Playwrightのテストとして実装され成功する(100%)。2026-09-21時点でゴールデンパス1〜7を`e2e/golden-paths.spec.ts`として実装し7件全成功。**ゴールデンパス8のみ例外**: wagmiの`mock`コネクタは常に`supportedChains`内のチェーンとして接続するため、「未対応ネットワークに接続している」状態をE2Eで再現できない。そのため`components/layout/NetworkBadge.test.tsx`のコンポーネントテストとして代替実装し(理由をコード内コメントに明記)、ロジックとしては同等にカバーしている。
- [x] **権限状態カバレッジ**: 「訪問者」「メンバー」「Owner」の3ロールそれぞれについて、表示されるUIが要件通りであることを確認するテストが存在する。ゴールデンパス1・5(訪問者)、3・4(メンバー)、2・6・7(Owner)がE2Eで各ロールのUI差分(役割バッジ・操作ボタンの有無)を検証している。

### 2.4 デプロイ可能性

- [x] フロントエンドが `next build` によるコンパイルに成功する(2026-09-21時点でローカル確認済み)。**Vercelへの実際のデプロイはまだ行っていない**(ビルド成功をもって「デプロイできる状態」とみなしている)。

### 2.5 全体

- [x] コントラクト・フロントエンド双方のテストがCI上で green であること。`.github/workflows/ci.yml`(`contracts`/`frontend-checks`/`frontend-e2e`の3ジョブ、いずれも`forge coverage`・Vitestカバレッジ80%閾値・E2E全件を含む)を実際にGitHub Actions上で実行し、2026-09-21時点で3ジョブとも成功(グリーン)を確認済み。
  - 初回実行で2件の不具合が判明し修正済み: (1) `tsc --noEmit`単体ではNext.jsが`.next/types/`に生成する`LayoutProps`等のアンビエント型を解決できず失敗する問題(`typecheck`スクリプトに`next typegen`を追加)。(2) E2Eの`getByText`が大文字小文字を無視した部分一致のため、意図しない要素と衝突しstrict-mode違反になっていた問題(該当箇所を`exact: true`に修正)。いずれもローカル環境に残っていた副産物(過去の`next build`実行で生成された`.next/types`、たまたま衝突しなかったタイミング)によりローカルでは検出できず、クリーンなCI環境で初めて顕在化した。
- [x] Lint・フォーマットチェック(Solidity: `forge fmt --check` / `forge lint`、フロントエンド: ESLint / Prettier)がエラーなしであること(2026-09-21時点でローカルおよびCI上の両方で確認済み)
- [x] 型チェック(TypeScript: `tsc --noEmit`)がエラーなしであること(2026-09-21時点でローカルおよびCI上の両方で確認済み)

## 3. なぜフロントエンドは「行カバレッジ100%」にしないのか

コントラクトと異なり、フロントエンドの行カバレッジ100%は次の理由で有効な指標にならないと考える。

- wagmiのフック(`useReadContract`/`useWriteContract`等)はローディング・エラー・成功など多数の内部分岐を持ち、その全パスを網羅しても実際の不具合検出にはほとんど寄与しない(ライブラリ側の責務であり、アプリ側でテストすべき対象ではない)。
- 行カバレッジは「そのコードが実行されたか」しか示さず、「ウォレット接続時にメンバー以外へ追加ボタンが表示されない」といった**要件との整合性**は保証しない。
- 100%を機械的に追い求めると、価値の低いテスト(実装の詳細をなぞるだけのテスト)を書くインセンティブが生まれる。

そのため、[要件定義4.1〜4.5](./001-mvp-requirements.md)に対応する実際のユーザー操作シナリオ(4章のゴールデンパス)を100%カバーすることを主指標とし、コードの機械的な網羅率(ユニットテストの80%)は補助指標として扱う。

## 4. ゴールデンパス一覧(E2Eテスト対象)

1. 未接続の訪問者がホーム画面を閲覧できる(Mission Log / Comms Channels / Party Roster、いずれも読み取り専用)
2. ウォレットを接続すると、自分のアドレスと接続中のネットワークが表示される
3. 既存メンバーが、他メンバーの承認を必要とせず単独で新規メンバーを追加できる
4. メンバーが自分自身を脱退(セルフ削除)できる
5. 非メンバー・非Ownerには、追加・削除系の操作UI(Add Member、チャットルーム/メッセージの編集ボタン等)が表示されない
6. Ownerがチャットルームを追加・削除できる
7. Ownerが理念・目標メッセージを追加・削除できる
8. コントラクト未対応のネットワークに接続している場合、適切な警告が表示され誤操作を防ぐ

## 5. オープン事項

- [x] E2Eテストにおけるウォレット接続のモック方法。2026-09-21時点で解決: ローカルAnvilチェーン + 実際の`Deploy.s.sol`によるデプロイ + wagmiの`mock`コネクタ(Owner・メンバー・非メンバーの3アカウント分、`?e2eConnector`クエリパラメータで選択)という構成で確定・実装済み(`e2e/global-setup.ts`)。
