# reports

ある時点でのテスト・検証結果のスナップショットを記録するディレクトリです。[Definition of Done](../requirements/002-definition-of-done.md)の達成状況を対外的に示す目的などで、日付ごとにファイルを追加していきます。

`docs/design/`や`docs/adr/`と異なり、内容を後から書き換えるのではなく、**日付ごとに新しいファイルを追加**します(過去のレポートは記録として残す)。

## 命名規則

`YYYY-MM-DD-test-results.md`
